import SafeAreaView from "@/widgets/SafeAreaView";
import MyCareer from "./[careerId]/container";

export default async function Page() {
  return (
    <SafeAreaView>
      <MyCareer />
    </SafeAreaView>
  );
}
