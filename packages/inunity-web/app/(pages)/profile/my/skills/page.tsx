import SafeAreaView from "@/widgets/SafeAreaView";
import MySkills from "./[skillId]/container";

export default async function Page() {
  return (
    <SafeAreaView>
      <MySkills />
    </SafeAreaView>
  );
}
